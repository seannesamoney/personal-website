# Sean Nesamoney — Personal Website

Personal portfolio built with Next.js and deployed from this repository.

## Local development

```bash
npm install
npm run dev
```

Then open http://localhost:3000.

## Editing workflow

This repository is the single source of truth for the website. Pushes to `main` can be automatically deployed by Vercel once the repository is connected there.
